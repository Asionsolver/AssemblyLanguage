;SUPPOSE AX AND BX CONTAIN SIGNED NIMBERS. WRITE SOME CODE TO PUT THE BIGGEST ONE IN CX.

.MODEL SMALL
.STACK 100H
.DATA
.CODE

MAIN PROC
    MOV CX,AX
    CMP BX,CX
    JLE NEXT
    MOV CX,BX
    MOV AH,4CH
    INT 21H

NEXT:
    ; Add your code here for the case when BX <= CX

    MOV AH,4CH
    INT 21H

MAIN ENDP

END MAIN